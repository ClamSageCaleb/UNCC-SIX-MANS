import { Client, Interaction, MessageActionRow, MessageButton, MessageEmbed } from "discord.js";


// collector.on('collect', async (i: { customId: string; update: (arg0: { content: string; components: never[]; }) => any; }) => {
//     if (i.customId === 'joinQueue') {
//         await i.update({ content: '${id} has been added to the queue', components: [] });
//     }
// });


